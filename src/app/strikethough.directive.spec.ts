import { StrikethoughDirective } from './strikethough.directive';

describe('StrikethoughDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikethoughDirective();
    expect(directive).toBeTruthy();
  });
});
